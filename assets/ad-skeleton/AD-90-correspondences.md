# 对应关系、对应规则与已知不一致（42010 6.9）

> **本节是逆向 AD 的重点。** 跨视图映射与规则违反是静态分析能精确给出、
> 而人手写不好也维护不住的东西；它还是唯一能固化成 CI 门禁、防止本 AD 腐化的部分。

## 1. 对应关系（6.9.2）

对应关系是 AD 元素之间「已标识或命名的关系」，可表达等价、组合、精化、一致性、追溯、
依赖、约束、满足、义务等。逐条给出参与元素与证据。

### 1.1 功能视图 ↔ 开发视图（实现关系）

| 功能元素 | 实现模块 | 证据 |
|---|---|---|
| <…> | <…> | `<path:line>` |

### 1.2 功能视图 ↔ 部署视图（部署关系）

| 功能元素 | 运行于 | 证据 |
|---|---|---|
| <…> | <进程/容器/节点> | `<path:line>` |

### 1.3 信息视图 ↔ 持久化（存储关系）

| 概念实体 | 存储形态 | 证据 |
|---|---|---|
| <…> | <表/文件格式/索引> | `<path:line>` |

### 1.4 与外部 AD 的对应（6.9.2 注记）

42010:2022 新增：**一份 AD 可以作为另一份 AD 中的 AD 元素**。用于表达与上游/同族项目的关系：

| 本 AD 元素 | 对应到 | 关系 | 证据 |
|---|---|---|---|
| <本项目的存储层> | <上游 X 项目的 lib/storage> | 复用（依赖） | `<go.mod / Cargo.toml 行号>` |

## 2. 对应规则（6.9.3）

规则是**可判定**的约束。官方模板要求：对每条规则，记录「规则成立」或**列出全部已知违反**——不许含糊。

| # | 规则 | 判定方式 | 结论 |
|---|---|---|---|
| R1 | <模块 A 不得依赖模块 B> | <grep / 依赖分析命令> | **成立** |
| R2 | <领域层不得引用 HTTP 类型> | <…> | **违反 2 处**（见 §2.1） |
| R3 | <包之间不得有循环依赖> | <…> | **成立** |
| R4 | <每个功能元素都映射到一个开发模块> | 核对 §1.1 | <…> |

### 2.1 违反明细

**R2 的违反**：

1. 【事实】<具体违反>（`path/file.ext:120`）
2. 【事实】<具体违反>（`path/file.ext:245`）

<不要为了让文档好看而修改措辞把违反掩盖过去——违反正是漂移的证据，记入 AD-99。>

### 2.2 判定命令（可复现）

```bash
# R1
<命令>
# R2
<命令>
```

## 3. 一致性分析与已知不一致（6.9.1）

官方模板的 must 项：**记录 AD 中任何已知的不一致**。标准承认「出于时间、精力或信息不足，
有时无法或不切实际地解决所有不一致」——允许存在，不允许隐瞒。

| # | 不一致 | 涉及的视图/元素 | 为何未解决 |
|---|---|---|---|
| I1 | <例：功能视图显示 A 直接调用 C，但部署视图显示两者不在同一网络域> | <…> | <需动态验证，本次未做> |

## 4. 固化成防腐层

**值得保持的规则应立刻固化成可执行检查**，否则本 AD 从写完那天起就开始过期。

已固化：

| 规则 | 固化方式 | 位置 |
|---|---|---|
| <R1> | <dependency-cruiser 规则 / ArchUnit 测试 / 断言脚本> | `<path>` |

未固化及原因：

- <R4：需要人工判断映射关系，暂无自动化方案>

<最低成本做法：不装任何工具，写一个断言测试遍历依赖并检查禁止边。十几行代码，
跟着仓库走，不引入新依赖。示例见 references/tooling.md。>
