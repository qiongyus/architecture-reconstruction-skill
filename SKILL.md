---
name: architecture-reconstruction
description: 从源代码重建既有系统的架构描述（Architecture Description, AD），产出内容按 ISO/IEC/IEEE 42010:2022 第 6 章的规范性内容项组织，每条断言附代码证据与置信度，拿不到的内容显式声明为缺口而不是编造。当用户要分析一个既有代码库或开源项目的架构、要给没有架构文档的系统补架构文档、要做逆向架构分析、问「这个项目的架构是怎样的」「帮我读懂这个仓库」、要产出架构视图（4+1/C4/逻辑视图/部署视图）、要写 SAD 或架构说明书、要评估某个库能否集成或 fork 后维护、要在重构前搞清现状结构时，都应当使用本 skill —— 即使用户没有说出「架构重建」「AD」「42010」这些词。不适用于为尚不存在的新系统做正向架构设计。
---

# 架构重建（Software Architecture Reconstruction）

## 这件事的本质

代码里没有「架构」这个现成的东西，只有**证据**。你要做的是从多种证据源交叉推断，重建出一份 **as-built（实际建成）** 的架构描述。

它和「as-intended（当初设想）」几乎总有差异——这个差异不是噪声，是本次工作最有价值的产出之一（Perry & Wolf 称之为架构侵蚀 erosion 与漂移 drift）。

**最危险的失败模式是编造动机。** 架构决策的「理由」原理上无法从代码恢复——学术界称之为架构知识蒸发（architectural knowledge vaporization）：架构知识大多是隐性的，只存在于创造者头脑中；代码显式记录了结构，但 rationale「缺失，或至多隐含在结构元素里」。一份读起来处处都有「之所以这样设计是为了……」却没有任何出处的重建文档，是在把猜测伪装成事实，比留白有害得多。

所以本 skill 的核心纪律不是产出更多内容，而是**让每条断言的认知地位可见**。

## 三级证据纪律（贯穿全部产出）

正文中每条实质性断言必须归入三类之一，并使用统一标记：

```markdown
【事实】写入路径的硬限制在 LogRows 层执行：字段名超 128 字节即丢弃
        （`lib/logstorage/log_rows.go:437`）

【推断】布隆过滤器的引入是为了降低查询时的块扫描量
        （依据：`bloomfilter.go` 仅被 `block_search.go` 的跳块判断调用，
         无其他调用点；置信度：高）

【缺口】选择「只分片、不复制」的理由未知
        （原因：仓库内无 ADR，commit 信息只描述 what 不含 why；
         可能获取途径：上游 GitHub issue 讨论、官方博客）
```

三条硬规则：

1. **`file:line` 是「事实」的唯一凭据。** 没有具体位置的陈述不得标为事实。行号会漂移，所以同时记录依据的 commit。
2. **「推断」必须写出推理链和置信度，不能只写结论。** 置信度判据见 `references/evidence-discipline.md`。
3. **「缺口」是一等产出，不是失败。** 尤其是 rationale 缺口——把它明确写出来，是在告诉读者「这里需要去问维护者」，这个信息本身有价值。

反过来说：如果你发现自己正在写一段没有任何证据支撑的设计意图，停下来，把它改写成【缺口】或标注为低置信度的【推断】。

## 结构服务于发现，不是相反

42010 的十个条目是**完备性检查表**，不是写作顺序，更不是价值排序。逆向分析真正值钱的产出是那些**可行动的尖锐发现**——静默的正确性问题、危险的默认值、会给出错误答案却无告警的路径、契约缺口与文档漂移。

### 怎么找：从「对外产生结果的路径」逐条扫，不要凭印象归类

**凭印象归类只能抓到读文档时撞见的东西。** 要做的是先机械地列出清单，再逐条盘问：

1. **列清单**（可机械得到）：HTTP 路由注册表、CLI 子命令、消息消费者、定时任务、导出/查询接口。
2. **对清单里的每一条，逐个问**：

   - **入参是否真的被读取**？把调用方能传的参数（query string、header、body 字段）与代码里实际解析的提取器对照。**未被读取的参数会被静默忽略**——调用方以为生效了，其实没有。
   - **上游信号是否被传递**？上游返回的错误码、部分结果标志、超时/截断标志，代码读了吗？没读就会把不完整当完整交给调用方。
   - **有没有硬编码的上限**？条数、大小、深度。超限时是明确报错，还是**静默给出基于截断数据的答案**？后者最危险。
   - **合并与排序是否守约**？多来源结果合并时，调用方要求的排序、过滤、分页还成立吗？扇出场景尤其容易丢。
   - **失败怎么表现**？错误被吞掉、被压成统一状态码、还是有指标与告警？被压平的错误会让调用方的重试与降级分支永远不触发。
   - **默认值是否反直觉**？缺省配置下的行为与调用方的合理预期是否一致。

3. **每条发现都写明「谁会因此做出错误判断」**——没有这句，它就只是个代码观察，不是架构发现。

这几类问题的共同点是：**代码不报错，答案却是错的**。它们不会出现在测试里，也不会出现在文档里，只能这样扫出来。

### 放在哪

放在**读者最先看到的位置**，不能被埋进按条款编号的小节里。速览档放在电梯描述之后；标准档在 `AD-00` 设一节「最危险的发现」并链接详情。

**自检**：如果读者只读你产出的前两屏，他能否看到那条最可能让他踩坑的事实？不能就重排。

十项都填满但每项空洞，不如条目齐备的同时把判断力用在这些地方——**条款完备性是下限，不是目标**。

## 产出什么：42010 第 6 章的内容项

42010:2022 第 4 章规定，一份 AD 声明一致性即须满足**第 6 章**的要求，且明确「tailoring 既不要求也不允许」。第 6 章共十项，这就是产出清单的依据——**条目一项都不能少，但承载形式可以随规模伸缩**。

| 条款 | 内容项 | 从代码的可恢复性 | 在重建型 AD 中怎么处理 |
|---|---|---|---|
| 6.1 | AD 标识与概述 | 完全可得 | 版本、commit、范围是事实；「本 AD 的目的」需人工给定（见 Step 0） |
| 6.2 | 利益相关者 stakeholders | 不可恢复 | 从代码痕迹反推「角色」，全篇标为推断 |
| 6.3 | 干系人视角 stakeholder perspectives | 不可恢复 | 用作**给 concerns 分组**的机制，从而组织视角 |
| 6.4 | 关注点 concerns | 部分可反推 | 代码里的显式投入是强证据；**优先级不可恢复** |
| 6.5 | 方面 aspects | 可反推 | 用作**细化 concern↔view 追溯**的机制 |
| 6.6 | 架构视角 viewpoints | 由你选择，非恢复 | 这是设计动作，须写明选择理由 |
| 6.7 | 架构视图 views | 可恢复 | 核心产出 |
| 6.8 | 视图构件 view components | 可恢复 | 核心产出。**注意 2022 版已用此词取代 architecture model**——文字、表格、清单与图同为一等构件 |
| 6.9 | 对应关系与规则 correspondences | **高度可恢复，逆向 AD 的独有强项** | 重点投入，见下 |
| 6.10 | 决策与理由 decisions & rationale | 结果可见，理由不可恢复 | 考古式 ADR，严格分「有据/推测/不可知」 |

三条由此推出的产出原则：

- **6.9 要重点做，不能像多数模板那样敷衍过去。** 跨视图映射是静态分析能精确给出、而人手写不好也维护不住的东西；它还是唯一能变成 CI 门禁、防止 AD 腐化的部分。做法见 Step 5。
- **6.2/6.3/6.10 结构性拿不到**，所以一份重建 AD 不可能「自然合规」。诚实的形态是「假说 + 显式缺口声明」，并在文档头一次性声明这个缺口，不在每节重复。
- **as-built 与 as-intended 的差异**是 42010 没有、但重建工作必须单列的产出。

各条款的 must/should 细节、官方模板的逐条硬要求，见 `references/ad-content-items.md`。

## 产出规模分级

先估规模再决定形态。条目不减，只改承载：

| 档位 | 触发条件 | 产出 |
|---|---|---|
| **速览** | 源文件 < 约 150，或用户只想「摸清这个库」 | 单文件 `ARCHITECTURE.md`，十个条目各一到两段 |
| **标准** | 源文件约 150–1500 | `AD-00-overview.md` / `AD-01-stakeholders-concerns.md` / `AD-02-viewpoints.md` / 每视图一文件 / `AD-90-correspondences.md` / `AD-91-decisions.md` / `AD-99-gaps.md` |
| **完整** | 源文件 > 约 1500，或多仓库/多语言 | 标准档 + 机器可读证据库（依赖图、端点清单、schema 导出）+ 一致性检查规则 |

在 `AD-00`（或速览档的文档头）**一次性声明**本次的档位、视角目录选择、以及 6.2/6.3/6.10 的缺口——声明一次全文生效，不要逐节重复偏离说明。

骨架模板在 `assets/ad-skeleton/`，直接复制填写。

## 工作流

### Step 0 — 先定这份 AD 的目的

42010 说 AD 是「为架构工作的特定目的而制作的工作产品」。目的不同，该恢复的视图完全不同：

| 目的 | 侧重 |
|---|---|
| 我要给这个项目贡献代码 | 开发视图、模块边界、构建与测试 |
| 我要 fork 后长期维护 | 全部视图 + 决策考古 + 漂移分析 |
| 我要评估能否集成 | 上下文视图、接口面、部署与运维约束 |
| 我要做安全审计 | 上下文视图、信息视图、认证授权路径 |
| 我要重构 | 模块边界 + 对应关系 + 变更热点 |

**用户没说清目的时，先问。** 这是唯一值得打断用户的问题——目的错了，后面全部白做。若用户明确要求不打断，默认按「贡献代码 + 长期维护」取并集，并在文档头声明这个假设。

### Step 1 — 证据清点与规模评估

先跑打包脚本，不要凭目录名猜：

```bash
bash scripts/inventory_evidence.sh <repo-root>
```

它报告八类证据源的存在情况与规模。**关键是它会告出「哪些证据源不存在」**——据此决定哪些结论根本没有依据可给。

证据源按性价比排序，以及各自的已知陷阱，见 `references/evidence-discipline.md`。这里只强调最容易出错的三条：

- **构建与依赖清单（`go.mod`/`Cargo.toml`/workspace 定义）是模块边界最硬的证据**，比目录结构可靠得多。目录结构可能已经腐化，只能当假设来源。
- **浅克隆会让 git 证据说谎。** 若 `.git/shallow` 存在，所有「改动频繁/很少改动」的结论必须标注历史截断日期，否则「这个模块很稳定」可能只是历史没拉全。脚本会检测并提示。
- **文字材料是唯一能恢复 rationale 的来源**：README、docs/、已有 ADR、设计提案（PEP/RFC/KEP 这类正式渠道）、重大 PR 讨论。必须去读；读不到就是【缺口】。

### Step 2 — 反推干系人与关注点（6.2–6.5）

代码里看不到干系人，只能看到**角色的痕迹**：对外 API 的消费者、运维端点、CLI 使用者、集群对等方、监控抓取方。从这些痕迹反推，全部标为【推断】。

42010 官方模板规定，下列类别**适用时必须识别**：users、operators、acquirers、owners、suppliers、developers、builders、maintainers。逐个过一遍——开源项目里 acquirers/owners 常常落到「上游维护者组织」，不要因为「代码里看不到」就跳过不写，那会漏掉真实的治理约束。

必须考虑的 concern 类别同样是硬性的：系统目的；架构对达成该目的的适宜性；构建与部署可行性；全生命周期对各方的风险与影响；如何维护与演进。

**concern 的强证据是「代码里的显式投入」**：布隆过滤器指向查询延迟，feature gate 指向可裁剪性，重试与熔断指向可用性，`_test` 里的压测指向性能目标。反过来，代码里**完全没有投入**的方面，说明它不是这个项目的 concern——这个否定结论同样有价值，值得写。

**优先级不可恢复**，不要伪造一张排好序的因素表。

perspectives 用来给 concerns 分组（如「运维类」「安全类」），aspects 用来细化 concern 与 view 的追溯。两者都不是装饰，见 `references/ad-content-items.md`。

产出必须包含 **concern↔stakeholder 追溯矩阵**（这是官方模板的 must 项，不是可选）。

### Step 3 — 选定视角目录并核对完备性（6.6）

42010 刻意不规定视图目录（Scope 明确不规定 models、notations、techniques），所以必须外挂一套。**默认用 Rozanski & Woods 的 7 视角 + perspective 机制**（Context / Functional / Information / Concurrency / Development / Deployment / Operational，叠加 Security、Performance & Scalability、Availability & Resilience、Evolution 等横切 perspective）——它是最贴近 42010 的实例化，且 perspective 机制正好对应 2022 版的 aspect。

其他可选目录（4+1、C4、SEI Views & Beyond）及各自的适用与短板，见 `references/viewpoint-catalogs.md`。用户指定了就用用户的；无论用哪套，都要在 `AD-02` 写明**为什么选它**（官方模板要求「Provide a rationale for each viewpoint used」）。

**然后跑完备性核对**——这是可机械判定的硬约束：

```bash
python3 scripts/check_coverage.py <ad-dir>
```

它检查官方模板那条 must：「每个已识别的 concern 必须至少被一个 viewpoint 框定」。没被任何视角框定的 concern，要么补视角，要么说明为何不覆盖。

**不要因为某套目录有 7 个视角就硬凑 7 个视图。** 视图数量由 concern 驱动：没有并发的项目不需要 Concurrency 视图，写一句「本系统为单线程，该视角不适用」比硬编一个空视图强。

### Step 4 — 建视图与视图构件（6.7 / 6.8）

每个视图一律包含四部分：

1. **视图构件**：图、表、清单、说明文字——2022 版明确文字与表格和图同为一等构件，不要为了「有图」而把该用表达的内容硬画成图。
2. **动机段**：这个结构为什么是这样。这是最容易漏、也最重要的一节；缺动机的架构文档让未来的修改者无法明智地改动。逆向场景下动机多为【推断】或【缺口】，照实标注。
3. **证据**：构件里每个元素的 `file:line`。
4. **Known Issues**：与视角约定的偏离、未覆盖区域、未决问题。

图的规模纪律：**每图只画 5–20 个承载「大思想」的元素**。判定方法是问「删掉哪个元素会损失大意思」——删了没损失的就该删。把所有包都画出来的图不是架构视图。

大仓库可以按视图并行取证（若当前环境支持子代理，一个视图一个探针，各自只回报结构化证据；不支持就顺序做）。取证工具按语言分列在 `references/tooling.md`。

### Step 5 — 对应关系与规则（6.9）：本工作的重点

这是逆向 AD 相对正向 AD 的强项，要认真做：

- **对应关系**：跨视图元素的映射。功能视图的组件 ↔ 开发视图的模块 ↔ 部署视图的进程；信息视图的实体 ↔ 持久化 schema。用表格记录，每行附证据。
- **对应规则**：可判定的约束。「模块 A 不得依赖 B」「领域层不得引用 HTTP 类型」「不得有循环依赖」。官方模板要求**逐条记录「规则成立」或「列出全部已知违反」**——不许含糊。
- **一致性分析与已知不一致**：官方模板的 must 项。发现的违反不要偷偷修正措辞掩盖过去，那正是漂移证据。

**把值得保持的规则立刻固化成可执行检查**（ArchUnit、dependency-cruiser、import-linter 等，按语言见 `references/tooling.md`），让 CI 阻止未来的违背。否则这份 AD 从写完那天就开始过期——重建一次成本很高，值得顺手装上防腐层。

### Step 6 — 决策考古（6.10）

只记录**关键**决策。官方模板给了选取判据：影响众多干系人、对项目计划至关重要、实施或强制执行代价高、对变更高度敏感或改动代价大、涉及复杂或非显然的推理、关系到架构显著需求。

每条决策的证据地位分四档。**关键是把「有据」拆成两级**——多数所谓的理由其实只是收益声明：

| 档 | 判据（对着出处逐条问） | 标注 |
|---|---|---|
| **有据·决策级** | 出处**指出了备选方案、或承认了接受的代价**。这才是 42010 说的 rationale | 引原文 |
| **有据·收益级** | 出处只说了「这样做有什么好处」，没有备选、没有代价 | 引原文，**并标明是收益声明** |
| **推测** | 从代码结构反推 | 写推理链 + 置信度 |
| **不可知** | 找不到理由。**照实写「理由未知，现状如此」** | 说明已查过哪些来源 |

**收益级不要当决策级用。** 面向用户的文档天然只写收益（它的目的是说服用户，不是记录审议），所以「This provides the following benefits」这类句子会大量出现且读起来很像理由。它能支撑「这个设计有什么好处」，支撑不了「当初为什么这么选」。

**判定时最容易犯的错**：把**变更描述**当成理由。commit body 写「Previously… After this pull request…」是在描述 diff，不是在解释动机——即使写得非常详尽。这种情况下真正的 why 通常在 commit 首行链接的 issue/PR 讨论里，**而那往往不在仓库内**：拿不到就是【缺口】，不要因为 body 很长就判成有据。

**另一个陷阱：有些「决策」不是决策，是继承的。** 项目从别处 fork 而来、或沿用了上游的既有结构时，事后文档会把现状描述得像深思熟虑的选择。查一下 git 的最初几个 commit——若某结构在第一个 commit 就存在，它多半是起点条件而非决策，应记在「约束与作用力」而不是「决策」里。

自检：如果你为某条决策写的理由里**既没有备选方案、也没有承认任何代价**，它就不是决策级证据。降档标注，不要含糊过去。

记录字段参照官方模板的建议项：决策标识、决策陈述、关联的 concerns、受影响的 AD 元素、理由、约束与作用力、假设、**考虑过的备选方案及其后果**。逆向场景下「备选方案」通常属【缺口】——除非在 PR 讨论里找到了争论记录，那是金矿。

**「决策」与「理由」的强制性不同**：42010:2011 明确规定，把决策整理成结构化记录是可选的（should），但**对每个关键架构决策给出理由是强制的**（官方模板 §1.3.3 标为 must，引 5.8.2；官方 FAQ 列举第 5 章 shalls 时也含此条）。2022 版把它拆成 6.10.1 决策记录与 6.10.2 理由记录，正文措辞无法从公开材料确证——**不要因为它位于第 6 章就断言强制**，第 6 章同时含 shall 与 should，只有 shall 项构成一致性要求。

实践上两版结论一致：**关键决策必须有理由**，所以照记不误；但引用条款时，2022 版 6.10 的具体措辞要标注为未确证。

### Step 7 — 缺口、不一致与漂移

单列一节，包含：

- **未覆盖区域**：哪些目录/子系统没看，为什么（时间、优先级、与目的无关）。
- **已知不一致**（6.9.1 的 must 项）。
- **as-built vs as-intended 漂移**：官方文档说架构是 X，代码实际是 Y。逐条列出，附两边证据。**这往往是整份文档最有行动价值的一节。**
- **契约缺口**：实现违背某类干系人利益之处（例如丢弃数据却无指标可观测、返回成功但未持久化）。这类发现是逆向分析独有的高价值产出，不要当普通备注埋没。

### Step 8 — 自检

交付前逐条核对，见 `references/ad-content-items.md` 末尾的检查清单。最低限度：

- [ ] 第 6 章十个条目**逐项**都有内容或有明确的「不适用/缺口」声明
- [ ] 每条实质断言都归入了事实/推断/缺口三类之一
- [ ] 所有【事实】都有 `file:line`，并记录了依据的 commit
- [ ] concern↔stakeholder 追溯矩阵存在
- [ ] 每个 concern 至少被一个 viewpoint 框定（跑 `check_coverage.py`）
- [ ] 每个视图都有动机段与 Known Issues
- [ ] 每张图元素数在 5–20，且删任一元素都会损失大意思
- [ ] 对应规则逐条给出「成立」或「违反清单」
- [ ] 决策分成了有据/推测/不可知三类，没有无出处的动机陈述
- [ ] 缺口与漂移单列成节，不是散落的脚注
- [ ] 浅克隆时 git 类结论标注了历史截断日期
- [ ] **最危险的发现出现在前两屏**，没有被条款结构挤到后面
- [ ] 静默正确性问题、危险默认值、无告警的错误路径各自检查过一遍

## 验证：不要停在纸上

重建结果需要验证，两个可行手段：

1. **找维护者 review** 是最快的验证。开源项目可以把关键推断整理成几个具体问题去 issue/discussion 里问。
2. **探针修改**：按你重建的架构理解去做一个小改动。如果改动波及范围与你的预测一致，说明理解是对的；如果处处意外，说明模块边界判断错了。

## 参考资料

需要更深内容时按需读取，不要一次性全读：

- `references/ad-content-items.md` — 42010 第 6 章逐条要求、官方模板的 must/should、完整自检清单
- `references/viewpoint-catalogs.md` — R&W / 4+1 / C4 / Views&Beyond 四套视角目录及选择指引
- `references/evidence-discipline.md` — 证据源清单与陷阱、置信度判据、标注约定
- `references/tooling.md` — 按语言的依赖分析与架构一致性检查工具

若当前项目存在实践规范库（常见路径 `standards/03-architecture-process/`，或 CLAUDE.md 中声明的 `$STD`），一并检索并在产出中标注其条目 ID，使结论可回溯到项目自己的规范依据。不存在则静默跳过——本 skill 自身完备，不依赖它。
